name: ICEBERG – Automated CVE Intelligence

on:
  schedule:
    - cron: "0 */6 * * *"   # Every 6 hours
  workflow_dispatch:        # Allow manual run

permissions:
  contents: write

jobs:
  iceberg:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests

      - name: Run ICEBERG intelligence
        env:
          NVD_API_KEY: ${{ secrets.NVD_API_KEY }}
          ENABLE_GITHUB_POC: "false"
        run: |
          python step3_merge_intel.py

      - name: Commit updated README
        run: |
          git config user.name "iceberg-bot"
          git config user.email "iceberg-bot@users.noreply.github.com"
          git add README_ICEBERG.md
          git commit -m "🔄 Update ICEBERG CVE intelligence" || echo "No changes"
          git push
